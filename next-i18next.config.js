/** @type {import('next-i18next').UserConfig} */
const i18nextConfig = {
    i18n: {
        defaultLocale: 'sk',
        locales: ['sk', 'en', 'de'],
        localeDetection: true
    }
};

export default i18nextConfig;
